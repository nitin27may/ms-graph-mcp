# Docs site toolchain. Kept out of pyproject.toml so it never lands in the
# published package's dependency tree.
#
#   uv venv .venv-docs --python 3.12
#   uv pip install --python .venv-docs/bin/python -r docs-requirements.txt
#   .venv-docs/bin/mkdocs serve
mkdocs>=1.6
mkdocs-material>=9.7.7
mkdocs-minify-plugin>=0.8
pymdown-extensions>=10
